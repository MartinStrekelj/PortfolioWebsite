import React from 'react';
import { BranchContainer } from './styled';

const Branch = ({title, description, image, linkURL, linkText}) => {
    return (
        <BranchContainer image={image}>
            
        </BranchContainer>
    )
}

export default Branch;

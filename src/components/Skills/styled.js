import styled from 'styled-components';
import {theme} from '../theme';

export const StyledSkills = styled.div`
    border: 2px solid ${theme.colors.dark_blue};
    border-radius: 5px;
    padding: ${theme.padding.md} ${theme.padding.lg};
`;